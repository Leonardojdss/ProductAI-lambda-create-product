name: Dev Pipeline Deploy

'on':
  push:
    branches:
      - dev

permissions:
  contents: read
  id-token: write

jobs:
  deploy-dev:
    uses: ./.github/workflows/deploy_pipeline.yml
    with:
      environment: dev
      aws-region: "us-east-1"
      aws-statefile-s3-bucket: "product-ai-leonardojdss"
    secrets:
      AWS_ASSUME_ROLE_ARN: ${{ secrets.AWS_ASSUME_ROLE_ARN }}